# hupselbrook — TOML project

Phase 4a TOML port of `../1.hupselbrook/`. Hand-translated from
`swap_linux.swp.template` + `swap.dra` + crop files. Full parity
against the legacy reader is the subject of the Phase 4a parity
test (`tests/unit/io/toml/test_hupselbrook_parity.pf`).
